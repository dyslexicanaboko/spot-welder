using Dapper;
using {{Namespace}}.Records;
using System.Collections.Generic;
using System.Data;
using System.Linq;
{{Namespaces}}

namespace {{Namespace}}.{{ContainingNamespace}}
{
  public class {{ClassName}}Repository
    : BaseRepository, I{{ClassName}}Repository
  {
    public {{ClassName}}Repository(IAppConfiguration configuration)
      : base(configuration)
    {
    }

    public [AO]{{RecordName}}?[AC] Read({{PrimaryKeyType}} {{PrimaryKeyParameter}})
    {
      const string sql = """
        SELECT
{{SelectAllList}}
        FROM {{Schema}}.{{Table}}
        WHERE {{PrimaryKeyColumn}} = @{{PrimaryKeyColumn}}
        """;

      [A]using var connection = await GetConnection();
      
      return connection.QuerySingleOrDefault[AS]<{{RecordName}}>(sql, GetPrimaryKeyParameter({{PrimaryKeyParameter}}));
    }

    public [AO]List<{{RecordName}}>[AC] ReadAll()
    {
      const string sql = """
        SELECT
{{SelectAllList}}
        FROM {{Schema}}.{{Table}}
        """;

      var connection = await GetConnection();
      
      return [AWO]connection.Query[AS]<{{RecordName}}>(sql)[AWC].AsList();
    }

    public [AO]{{PrimaryKeyType}}[AC] Insert({{RecordName}} entity)
    {
      const string sql = """
        INSERT INTO {{Schema}}.{{Table}} (
{{InsertColumnList}}
        ) VALUES (
{{InsertValuesList}})  {{ScopeIdentity}}
        """;

      [A]using var connection = await GetConnection();

      var p = new DynamicParameters();
  {{DynamicParametersInsert}}
        
  {{PrimaryKeyInsertExecution}}
    }

    public [void] Update({{RecordName}} entity)
    {
      const string sql = """
        UPDATE {{Schema}}.{{Table}} SET
{{UpdateParameters}}
        WHERE {{PrimaryKeyColumn}} = @{{PrimaryKeyColumn}}
        """;

      [A]using var connection = await GetConnection();

      var p = new DynamicParameters();
  {{DynamicParametersUpdate}}

      [A]connection.Execute[AS](sql, p);
    }

    public [void] Delete({{PrimaryKeyType}} {{PrimaryKeyParameter}})
    {
      const string sql = """
        DELETE FROM {{Schema}}.{{Table}}
        WHERE {{PrimaryKeyColumn}} = @{{PrimaryKeyColumn}}
        """;

      [A]using var connection = await GetConnection();

      [A]connection.Execute[AS](sql, GetPrimaryKeyParameter({{PrimaryKeyParameter}}));
    }

    private static DynamicParameters GetPrimaryKeyParameter({{PrimaryKeyType}} {{PrimaryKeyParameter}})
    {
      var p = new DynamicParameters();
      p.Add(name: "@{{PrimaryKeyColumn}}", dbType: DbType.{{PrimaryKeyDbType}}, value: {{PrimaryKeyParameter}});
      
      return p;
    }
  }
}
