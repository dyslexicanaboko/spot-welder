{{UsingDirectives}}
using Microsoft.AspNetCore.JsonPatch;
using Microsoft.AspNetCore.Mvc;

namespace {{AbsoluteContainingNamespace}};

[Route("api/v1/{{ApiRoute}}")]
[ApiController]
public class {{SubjectName}}Controller
  : BaseApiController
{
  private readonly I{{SubjectName}}Mapper _mapper;

  private readonly I{{SubjectName}}Manager _manager;

  public {{SubjectName}}Controller(
    I{{SubjectName}}Manager manager,
    I{{SubjectName}}Mapper mapper)
  {
    _manager = manager;

    _mapper = mapper;
  }

  // GET api/v1/{{ApiRoute}}/5
  [HttpGet("{id:{{PrimaryKeyType}}}")]
  [ProducesResponseType(StatusCodes.Status200OK, Type = typeof({{SubjectName}}V1Model))]
  [ProducesResponseType(StatusCodes.Status404NotFound, Type = typeof(ErrorModel))]
  public async Task<ActionResult<{{SubjectName}}V1Model>> Get({{PrimaryKeyType}} id)
  {
    var entity = [AMO]_manager.Get(id)[AMC];

    if (entity == null) throw new Exception($"{{SubjectName}}:{id} not found.");

    return Ok(_mapper.ToModel(entity));
  }

  // GET api/v1/{{ApiRoute}}
  [HttpGet]
  [ProducesResponseType(StatusCodes.Status200OK, Type = typeof({{SubjectName}}V1Model))]
  public async Task<ActionResult<{{SubjectName}}V1Model>> GetAll()
  {
    var entity = [AMO]_manager.GetAll()[AMC];

    return Ok(_mapper.ToModel(entity));
  }
  
  // POST api/v1/{{ApiRoute}}
  [HttpPost]
  [ProducesResponseType(StatusCodes.Status201Created, Type = typeof({{SubjectName}}V1CreatedModel))]
  [ProducesResponseType(StatusCodes.Status400BadRequest, Type = typeof(ErrorModel))]
  [ProducesResponseType(StatusCodes.Status404NotFound, Type = typeof(ErrorModel))]
  [ProducesResponseType(StatusCodes.Status500InternalServerError, Type = typeof(ErrorModel))]
  public async Task<ActionResult<{{SubjectName}}V1CreatedModel>> Post([FromBody] {{SubjectName}}V1CreateModel model)
  {
    var entity = _mapper.ToEntity(model);

    if (entity == null) throw new Exception("Model cannot be null.");

    var result = [AMO]_manager.Add(entity)[AMC];

    var m = _mapper.ToModel(result);

    return CreatedAtAction(nameof(Get), new { id = m!.{{PrimaryKeyProperty}} }, m);
  }
  
  // PATCH api/v1/{{ApiRoute}}/5
  [HttpPatch("{id:{{PrimaryKeyType}}}")]
  [ProducesResponseType(StatusCodes.Status204NoContent)]
  [ProducesResponseType(StatusCodes.Status400BadRequest, Type = typeof(ErrorModel))]
  [ProducesResponseType(StatusCodes.Status404NotFound, Type = typeof(ErrorModel))]
  [ProducesResponseType(StatusCodes.Status500InternalServerError, Type = typeof(ErrorModel))]
  public async Task<ActionResult> Patch({{PrimaryKeyType}} id, [FromBody] JsonPatchDocument<{{SubjectName}}V1PatchModel> patchDoc)
  {
    var db = [AMO]_manager.Get(id)[AMC];
          
    var model = _mapper.ToPatchModel(db);

    if (model == null) throw new Exception($"{{SubjectName}}:{id} not found.");

    patchDoc.ApplyTo(model);

    var entity = _mapper.ToEntity(model);

    [AMO]_manager.Edit(entity)[AMC];

    return NoContent();
  }

  // DELETE api/v1/{{ApiRoute}}/5
  [HttpDelete("{id:{{PrimaryKeyType}}}")]
  [ProducesResponseType(StatusCodes.Status204NoContent)]
  public async Task<ActionResult> Delete({{PrimaryKeyType}} id)
  {
    [AMO]_manager.Remove(id)[AMC];

    return NoContent();
  }
}

